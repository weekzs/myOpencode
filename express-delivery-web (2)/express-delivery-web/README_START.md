# 🚀 快速启动指南

## 方式一：一键启动（推荐）
双击运行 `start.bat` 文件，即可自动启动前后端服务。

## 方式二：手动启动
如果需要分别控制前后端，可以使用以下方式：

### 启动后端
```bash
cd backend
start-backend.bat
# 或手动执行: npx ts-node src/server.ts
```

### 启动前端
```bash
cd frontend
start-frontend.bat
# 或手动执行: npm run dev
```

## 🌐 访问地址
- **前端应用**: http://localhost:3000
- **后端API**: http://localhost:3001
- **健康检查**: http://localhost:3001/health

## 🔧 故障排除

### 端口被占用
如果遇到端口占用问题，启动脚本会自动清理之前的进程。

### 手动清理进程
如果启动脚本无法清理进程，可以手动执行：
```bash
# Windows
taskkill /f /im node.exe

# 清理Next.js缓存
cd frontend && rmdir /s /q .next
```

## 📝 开发说明
- 后端使用TypeScript + Express + Prisma
- 前端使用Next.js + React + Tailwind CSS
- 数据库使用MySQL